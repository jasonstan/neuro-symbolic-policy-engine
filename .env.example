# Copy to .env and fill in. .env is gitignored.
# Only required when running the judgment rail against the real Anthropic API
# (CLI defaults to --llm=fake; tests never hit the network).
ANTHROPIC_API_KEY=

# Optional override; defaults to claude-sonnet-4-6.
NSPK_JUDGMENT_MODEL=claude-sonnet-4-6
